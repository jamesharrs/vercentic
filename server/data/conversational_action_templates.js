// server/data/conversational_action_templates.js
// ═══════════════════════════════════════════════════════════════════════════
// CONVERSATIONAL ACTION TEMPLATES — the 10 starting use cases for headless
// (Slack / Microsoft Teams) access to Vercentic.
//
// Offered in the builder as one-click starting points, same pattern as
// data/agent_templates.js. Cloning a template creates a normal row in the
// `conversational_actions` table that the admin can then edit freely.
//
// permission_required maps 1:1 onto the existing RBAC model
// (middleware/rbac.js GLOBAL_ACTIONS / object permissions) — nothing new is
// invented, so a use case can never grant more than the underlying
// permission already allows for that user's role.
// ═══════════════════════════════════════════════════════════════════════════

module.exports = [
  {
    id: 'tpl_search_candidates',
    category: 'search', category_label: 'Search & Lookup', category_color: '#3b5bdb', category_icon: 'search',
    name: 'Search Candidates / Jobs',
    description: 'Free-text search across People and Jobs, returned as cards you can tap to open in Vercentic.',
    use_case: 'The most common ask — "show me candidates for X", "any open reqs in Dubai".',
    trigger_type: 'intent',
    trigger_phrases: [
      'show me candidates for {job_title}',
      'find candidates in {location}',
      'any open jobs in {department}',
      'search for {name}',
      'who applied for {job_title}',
    ],
    parameters: [
      { key: 'object', label: 'Object to search', type: 'select', required: true, extract_hint: 'people or jobs' },
      { key: 'query',  label: 'Search text',      type: 'text',   required: true, extract_hint: 'name, title, location, skill etc.' },
    ],
    action_type: 'search_records',
    action_config: { default_object_slug: 'people', max_results: 5 },
    permission_required: { type: 'object', object_slug: 'people', action: 'view' },
    approval_required: false,
    rate_limit_per_user_per_hour: 60,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'list',
    card_type: 'record_summary',
    response_template: null,
  },

  {
    id: 'tpl_update_stage',
    category: 'pipeline', category_label: 'Pipeline Actions', category_color: '#0ca678', category_icon: 'move',
    name: 'Update Workflow Stage',
    description: 'Move a candidate to a new stage on their linked job pipeline, right from chat.',
    use_case: '"Move Sarah Chen to Interview stage" — no need to open the app.',
    trigger_type: 'intent',
    trigger_phrases: [
      'move {candidate_name} to {stage} stage',
      'advance {candidate_name} to {stage}',
      'update {candidate_name}\'s stage to {stage}',
    ],
    parameters: [
      { key: 'candidate_name', label: 'Candidate', type: 'people', required: true, extract_hint: 'name of the person record' },
      { key: 'stage',          label: 'New stage',  type: 'text',   required: true, extract_hint: 'target pipeline stage name' },
    ],
    action_type: 'update_stage',
    action_config: {},
    permission_required: { type: 'global', action: 'record_move_stage' },
    approval_required: false,
    rate_limit_per_user_per_hour: 60,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'text',
    card_type: null,
    response_template: '✅ Moved *{{candidate_name}}* to *{{stage}}*.',
  },

  {
    id: 'tpl_my_digest',
    category: 'digest', category_label: 'Digests & Reminders', category_color: '#7c3aed', category_icon: 'sun',
    name: 'My Daily Digest',
    description: 'A personal "what needs my attention today" summary — pending approvals, interviews today, stale candidates.',
    use_case: '"What\'s on my plate today" — the morning check-in use case.',
    trigger_type: 'command',
    trigger_phrases: ['my digest', 'what\'s on my plate', 'daily summary', 'my day'],
    parameters: [],
    action_type: 'my_digest',
    action_config: { include: ['interviews_today', 'pending_approvals', 'stale_candidates'] },
    permission_required: { type: 'global', action: 'access_dashboard' },
    approval_required: false,
    rate_limit_per_user_per_hour: 30,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'card',
    card_type: 'digest',
    response_template: null,
  },

  {
    id: 'tpl_approval_inbox',
    category: 'approvals', category_label: 'Approvals', category_color: '#e8590c', category_icon: 'check-circle',
    name: 'Approve / Reject Inbox Item',
    description: 'Approve or decline a pending approval request directly from a Teams/Slack card — no email, no app.',
    use_case: 'Best "wow" demo moment: approve an offer from a push notification.',
    trigger_type: 'event',
    trigger_event: 'approval_requested',
    trigger_event_config: { sources: ['approvals'] },
    parameters: [
      { key: 'target_id', label: 'Approval ID', type: 'text',   required: true },
      { key: 'decision',  label: 'Decision',    type: 'select', required: true, extract_hint: 'approve or decline' },
    ],
    action_type: 'approval_action',
    action_config: {},
    permission_required: { type: 'global', action: 'manage_settings' },
    approval_required: false,
    rate_limit_per_user_per_hour: 100,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'card',
    card_type: 'approval_result',
    response_template: null,
  },

  {
    id: 'tpl_schedule_interview',
    category: 'pipeline', category_label: 'Pipeline Actions', category_color: '#0ca678', category_icon: 'calendar',
    name: 'Schedule an Interview',
    description: 'Book an interview for a candidate, asking follow-up questions for anything missing (date, type, interviewer).',
    use_case: 'Multi-turn: "book Sarah for a technical interview Thursday" → bot fills the gaps.',
    trigger_type: 'intent',
    trigger_phrases: [
      'schedule an interview for {candidate_name}',
      'book {candidate_name} for {interview_type}',
      'set up an interview with {candidate_name} on {date}',
    ],
    parameters: [
      { key: 'candidate_name', label: 'Candidate',      type: 'people', required: true },
      { key: 'interview_type', label: 'Interview type', type: 'text',   required: true },
      { key: 'date',           label: 'Date',           type: 'date',   required: true },
      { key: 'time',           label: 'Time',           type: 'text',   required: false },
      { key: 'interviewer',    label: 'Interviewer',    type: 'people', required: false },
    ],
    action_type: 'schedule_interview',
    action_config: {},
    permission_required: { type: 'global', action: 'manage_interviews' },
    approval_required: false,
    rate_limit_per_user_per_hour: 30,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'card',
    card_type: 'interview_confirmation',
    response_template: null,
  },

  {
    id: 'tpl_ai_summary',
    category: 'ai', category_label: 'AI-Assisted', category_color: '#b45309', category_icon: 'sparkles',
    name: 'AI Candidate Summary',
    description: 'Ask for a quick AI-generated fit summary of a candidate against the role they applied for.',
    use_case: '"Summarise this candidate\'s fit" before jumping into a screening call.',
    trigger_type: 'intent',
    trigger_phrases: [
      'summarise {candidate_name}',
      'what do we know about {candidate_name}',
      'is {candidate_name} a good fit for {job_title}',
    ],
    parameters: [
      { key: 'candidate_name', label: 'Candidate', type: 'people', required: true },
    ],
    action_type: 'ai_summary',
    action_config: {},
    permission_required: { type: 'global', action: 'access_copilot' },
    approval_required: false,
    rate_limit_per_user_per_hour: 20,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'card',
    card_type: 'ai_summary',
    response_template: null,
  },

  {
    id: 'tpl_add_note',
    category: 'pipeline', category_label: 'Pipeline Actions', category_color: '#0ca678', category_icon: 'edit',
    name: 'Log an Activity / Note',
    description: 'Log a quick note on a candidate record — no approval needed, low risk.',
    use_case: '"Log: spoke with John, strong on system design" right after a call.',
    trigger_type: 'intent',
    trigger_phrases: [
      'log a note on {candidate_name}: {note_text}',
      'add note to {candidate_name} {note_text}',
      'note for {candidate_name}: {note_text}',
    ],
    parameters: [
      { key: 'candidate_name', label: 'Candidate', type: 'people', required: true },
      { key: 'note_text',      label: 'Note',      type: 'textarea', required: true },
    ],
    action_type: 'add_note',
    action_config: {},
    permission_required: { type: 'global', action: 'record_add_note' },
    approval_required: false,
    rate_limit_per_user_per_hour: 60,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'text',
    card_type: null,
    response_template: '📝 Note added to *{{candidate_name}}*.',
  },

  {
    id: 'tpl_proactive_alert',
    category: 'alerts', category_label: 'Proactive Alerts', category_color: '#c2255c', category_icon: 'bell',
    name: 'Proactive New-Applicant Alert',
    description: 'Bot posts unprompted into a channel whenever a new applicant lands on a watched job.',
    use_case: 'The one use case that isn\'t triggered by a message at all — a system event pushes into chat.',
    trigger_type: 'event',
    trigger_event: 'record_created',
    trigger_event_config: { object_slug: 'people', condition: { field: 'source', op: 'equals', value: 'career_site' } },
    parameters: [],
    action_type: 'proactive_notify',
    action_config: { post_to: 'default_channel' },
    permission_required: { type: 'object', object_slug: 'people', action: 'view' },
    approval_required: false,
    rate_limit_per_user_per_hour: null,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'card',
    card_type: 'new_applicant',
    response_template: null,
  },

  {
    id: 'tpl_report_query',
    category: 'reporting', category_label: 'Reporting', category_color: '#1971c2', category_icon: 'bar-chart-2',
    name: 'Ask a Reporting Question',
    description: 'Simple aggregation questions answered from live data — headcount, open reqs, pipeline counts.',
    use_case: '"How many open reqs does my team have" without opening Reports.',
    trigger_type: 'intent',
    trigger_phrases: [
      'how many open jobs in {department}',
      'how many candidates in {stage}',
      'headcount for {department}',
      'open reqs for my team',
    ],
    parameters: [
      { key: 'metric', label: 'Metric', type: 'select', required: true, extract_hint: 'open_jobs, candidate_count, headcount' },
      { key: 'filter_field', label: 'Filter field', type: 'text', required: false },
      { key: 'filter_value', label: 'Filter value', type: 'text', required: false },
    ],
    action_type: 'report_query',
    action_config: {},
    permission_required: { type: 'global', action: 'run_reports' },
    approval_required: false,
    rate_limit_per_user_per_hour: 30,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'text',
    card_type: null,
    response_template: null,
  },

  {
    id: 'tpl_bulk_add_pool',
    category: 'bulk', category_label: 'Bulk Actions', category_color: '#9c36b5', category_icon: 'layers',
    name: 'Bulk Add to Talent Pool',
    description: 'Add several named candidates to a talent pool in one command — higher blast radius, kept behind approval by default.',
    use_case: '"Add these 3 candidates to the Sales Pool" after a sourcing session.',
    trigger_type: 'intent',
    trigger_phrases: [
      'add {candidate_names} to {pool_name} pool',
      'add these candidates to {pool_name}',
    ],
    parameters: [
      { key: 'candidate_names', label: 'Candidates', type: 'people', required: true, extract_hint: 'comma-separated list of names' },
      { key: 'pool_name',       label: 'Talent pool', type: 'text',  required: true },
    ],
    action_type: 'bulk_add_to_pool',
    action_config: {},
    permission_required: { type: 'global', action: 'bulk_actions' },
    approval_required: true,
    rate_limit_per_user_per_hour: 10,
    channels: ['slack', 'microsoft_teams'],
    response_type: 'card',
    card_type: 'bulk_result',
    response_template: null,
  },
];
