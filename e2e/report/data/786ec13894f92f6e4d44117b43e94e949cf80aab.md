# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: core/auth.spec.js >> Login flow >> valid credentials → lands on main app
- Location: e2e/core/auth.spec.js:6:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: page.click: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('button[type="submit"]')

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - generic [ref=e4]:
    - generic [ref=e7]:
      - generic [ref=e8]:
        - img [ref=e9]
        - generic [ref=e19]: Vercentic
      - heading "The modern people platform." [level=1] [ref=e20]:
        - text: The modern
        - text: people platform.
      - paragraph [ref=e21]: Intelligence built in. Not bolted on. Configure, manage, report and deliver — in one continuous loop.
      - generic [ref=e22]: 100% AI-powered, infinitely configurable
      - generic [ref=e24]: Built for enterprise TA and RPO teams
      - generic [ref=e26]: One organic platform — not a stack of tools
    - generic [ref=e28]: Vercentic · The modern people platform · 2026
  - generic [ref=e30]:
    - heading "Welcome back" [level=2] [ref=e31]
    - paragraph [ref=e32]: Sign in to your Vercentic workspace
    - generic [ref=e33]:
      - generic [ref=e34]: Email address
      - textbox "you@organisation.com" [ref=e35]: admin@talentos.io
    - generic [ref=e36]:
      - generic [ref=e37]: Password
      - textbox "••••••••" [active] [ref=e38]: Admin1234!
    - button "Sign in" [ref=e39] [cursor=pointer]:
      - img [ref=e40]
      - text: Sign in
    - generic [ref=e44]: OR
    - button "Continue with SSO" [ref=e46] [cursor=pointer]:
      - img [ref=e47]
      - text: Continue with SSO
    - paragraph [ref=e50]: Forgot password? · Contact your admin
```

# Test source

```ts
  1  | // e2e/core/auth.spec.js
  2  | // Authentication flow — login, session persistence, logout.
  3  | const { test, expect, login, logout, DEFAULT_EMAIL, DEFAULT_PASSWORD } = require('../fixtures/auth');
  4  | 
  5  | test.describe('Login flow', () => {
  6  |   test('valid credentials → lands on main app', async ({ page }) => {
  7  |     await page.goto('/');
  8  |     await page.fill('input[type="email"], input[name="email"]', DEFAULT_EMAIL);
  9  |     await page.fill('input[type="password"], input[name="password"]', DEFAULT_PASSWORD);
> 10 |     await page.click('button[type="submit"]');
     |                ^ Error: page.click: Test timeout of 30000ms exceeded.
  11 | 
  12 |     // Should navigate away from login page
  13 |     await page.waitForURL(url => !url.pathname.includes('login'), { timeout: 15000 });
  14 |     // Dashboard or main nav should be visible
  15 |     await expect(page.locator('nav, [data-testid="sidebar"], text=Dashboard').first()).toBeVisible({ timeout: 10000 });
  16 |   });
  17 | 
  18 |   test('wrong password → shows error, stays on login', async ({ page }) => {
  19 |     await page.goto('/');
  20 |     await page.fill('input[type="email"], input[name="email"]', DEFAULT_EMAIL);
  21 |     await page.fill('input[type="password"], input[name="password"]', 'WrongPassword999!');
  22 |     await page.click('button[type="submit"]');
  23 | 
  24 |     // Should remain on login page
  25 |     await page.waitForTimeout(2000);
  26 |     const url = page.url();
  27 |     // Either still shows login form or shows an error
  28 |     const hasError = await page.locator('text=Invalid, text=incorrect, text=wrong, [role="alert"]').first().isVisible().catch(() => false);
  29 |     const stillOnLogin = await page.locator('input[type="password"]').isVisible().catch(() => false);
  30 |     expect(hasError || stillOnLogin).toBeTruthy();
  31 |   });
  32 | 
  33 |   test('session persists after page refresh', async ({ page }) => {
  34 |     await login(page, DEFAULT_EMAIL, DEFAULT_PASSWORD);
  35 |     const urlBefore = page.url();
  36 | 
  37 |     await page.reload();
  38 |     await page.waitForLoadState('networkidle');
  39 | 
  40 |     // Should still be logged in (not redirected to login)
  41 |     const stillOnLogin = await page.locator('input[type="password"]').isVisible({ timeout: 3000 }).catch(() => false);
  42 |     expect(stillOnLogin).toBe(false);
  43 |   });
  44 | 
  45 |   test('logout clears session — next visit goes to login', async ({ page }) => {
  46 |     await login(page, DEFAULT_EMAIL, DEFAULT_PASSWORD);
  47 |     await logout(page);
  48 | 
  49 |     await page.goto('/');
  50 |     await page.waitForLoadState('networkidle');
  51 | 
  52 |     // Should be redirected to login or see login form
  53 |     const loginVisible = await page.locator('input[type="email"], input[name="email"]').isVisible({ timeout: 10000 }).catch(() => false);
  54 |     expect(loginVisible).toBe(true);
  55 |   });
  56 | });
  57 | 
  58 | test.describe('Route protection', () => {
  59 |   test('unauthenticated user cannot access protected API routes', async ({ request }) => {
  60 |     // Direct API call without cookies — should get 401
  61 |     const res = await request.get('/api/records?object_id=fake&environment_id=fake');
  62 |     expect(res.status()).toBe(401);
  63 |   });
  64 | });
  65 | 
```