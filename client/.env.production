# client/.env.production
# Baked into the JS bundle at build time by Vite.
# Direct to Railway — bypasses Vercel edge proxy entirely.
# Switch to https://api.vercentic.com once the custom domain is configured.
VITE_API_URL=https://talentos-production-4045.up.railway.app
