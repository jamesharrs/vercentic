# client/.env.production
# Baked into the JS bundle at build time by Vite.
# API calls go direct to Railway via custom domain — bypasses Vercel edge proxy entirely.
VITE_API_URL=https://api.vercentic.com
