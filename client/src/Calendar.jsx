// Calendar.jsx — re-exports CalendarView as the default calendar experience
// Tasks and events are surfaced via CalendarView's interview grid;
// standalone task/event creation available via the + button on each day.
export { default } from "./CalendarView.jsx";
